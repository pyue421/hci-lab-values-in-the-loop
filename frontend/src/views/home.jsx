import React from "react"

export default function Home() {
  return (
    <div style={{maxWidth: 900, margin: "40px auto", background: "white", padding: 24, borderRadius: 16}}>
      <h2>Home (placeholder)</h2>
      <p>You finished onboarding. Next step is to build the map + calendar + matches screen.</p>
    </div>
  )
}
